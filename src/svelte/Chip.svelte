<script lang="ts">export let selected = false; export let color: "primary" | "neutral" = "neutral"; export let size: "sm" | "md" = "md"; export let className = ""; export let type: "button" | "submit" | "reset" = "button";</script><button {type} class={`haki-chip ${selected || color === "primary" ? "haki-chip--primary" : ""} ${size === "sm" ? "haki-chip--sm" : ""} ${className}`.trim()} aria-pressed={selected} {...$$restProps}><slot /></button>
